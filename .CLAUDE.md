# Lotto Simulator - Claude Code 가이드

## 프로젝트 개요

로또 시뮬레이터 웹 애플리케이션. Shadcn Admin Dashboard를 기반으로 로또 번호 생성, 시뮬레이션, 통계 분석, 댓글/좋아요 기능을 제공합니다.

## 기술 스택

### Frontend
- **Framework**: React 19 + TypeScript
- **Build Tool**: Vite 7
- **Routing**: TanStack Router
- **State Management**: TanStack Query, Zustand
- **UI Components**: Shadcn UI (TailwindCSS + RadixUI)
- **Icons**: Lucide Icons

### Backend
- **API Server**: Hono (Vite dev server 통합)
- **Database**: Neon PostgreSQL (Serverless)
- **ORM**: Drizzle ORM
- **Auth**: Clerk

### Testing
- **Test Runner**: Vitest
- **Testing Library**: @testing-library/react

## 프로젝트 구조

```
src/
├── components/          # 공통 UI 컴포넌트
│   └── ui/             # Shadcn UI 컴포넌트
├── db/                 # 데이터베이스 설정
│   ├── index.ts        # Drizzle 클라이언트
│   └── schema.ts       # DB 스키마 (users, comments, likes)
├── features/           # 기능별 모듈
│   ├── comments/       # 댓글 기능
│   │   ├── components/ # 댓글 컴포넌트
│   │   ├── hooks/      # 댓글 관련 훅
│   │   ├── api.ts      # API 호출 함수
│   │   └── types.ts    # 타입 정의
│   ├── dashboard/      # 대시보드
│   ├── lotto/          # 로또 기능
│   └── settings/       # 설정 페이지
├── routes/             # TanStack Router 라우트
├── server/             # Hono API 서버
│   ├── index.ts        # 서버 진입점
│   └── routes/         # API 라우트
│       ├── comments.ts # 댓글 CRUD API
│       ├── likes.ts    # 좋아요 API
│       └── users.ts    # 사용자 동기화 API
├── hooks/              # 공통 훅
├── lib/                # 유틸리티 라이브러리
└── utils/              # 유틸리티 함수
```

## 주요 명령어

```bash
# 개발 서버 실행
npm run dev

# 빌드
npm run build

# 테스트 실행
npm test              # Vitest 테스트 (API 라우트)
npm run test:watch    # 감시 모드
npm run test:legacy   # 기존 Node.js 테스트 (로또 로직)

# 린팅/포맷팅
npm run lint
npm run format

# 데이터베이스
npx drizzle-kit generate  # 마이그레이션 생성
npx drizzle-kit migrate   # 마이그레이션 실행
npx drizzle-kit push      # 스키마 푸시
```

## API 엔드포인트

### Comments API
- `GET /api/comments` - 댓글 목록 조회
- `POST /api/comments` - 댓글 생성
- `PUT /api/comments/:id` - 댓글 수정
- `DELETE /api/comments/:id` - 댓글 삭제

### Likes API
- `POST /api/likes/toggle` - 좋아요 토글
- `GET /api/likes/count/:targetType/:targetId` - 좋아요 수 조회
- `GET /api/likes/check/:targetType/:targetId/:userId` - 좋아요 여부 확인
- `GET /api/likes/user/:userId` - 사용자 좋아요 목록

### Users API
- `POST /api/users/sync` - Clerk 사용자 동기화
- `GET /api/users/clerk/:clerkId` - Clerk ID로 사용자 조회

## 데이터베이스 스키마

### users
- `id` (UUID, PK)
- `clerkId` (TEXT, UNIQUE) - Clerk 사용자 ID
- `username` (TEXT)
- `imageUrl` (TEXT)
- `createdAt`, `updatedAt` (TIMESTAMP)

### comments
- `id` (UUID, PK)
- `userId` (UUID, FK → users)
- `content` (TEXT)
- `parentId` (UUID, nullable) - 대댓글용
- `createdAt`, `updatedAt` (TIMESTAMP)

### likes
- `userId` (UUID, FK → users)
- `targetId` (UUID) - 댓글 또는 시뮬레이션 ID
- `targetType` (ENUM: 'comment' | 'simulation')
- Composite PK: (userId, targetId, targetType)

## 개발 규칙

### 코드 스타일
- TypeScript strict mode 사용
- 컴포넌트는 함수형 컴포넌트 사용
- 커스텀 훅은 `use-` 접두사 사용
- API 호출은 TanStack Query 사용

### 커밋 메시지
```
<type>: <description>

- feat: 새 기능
- fix: 버그 수정
- refact: 리팩토링
- test: 테스트
- docs: 문서
- style: 코드 스타일
- chore: 기타
```

### 테스트
- API 라우트는 Vitest로 테스트
- 데이터베이스 모킹 사용
- 에러 핸들링 테스트 포함

## 환경 변수

```env
# Database (Neon PostgreSQL)
DATABASE_URL=postgresql://...

# Clerk Auth
VITE_CLERK_PUBLISHABLE_KEY=pk_...
CLERK_SECRET_KEY=sk_...
```

## 성능 최적화

### 번들 최적화
- `vite.config.ts`에서 `manualChunks`로 벤더 청크 분리
- 불필요한 의존성 제거 (`npm run knip`으로 확인)

### React 최적화
- `React.memo`: 반복 렌더링되는 컴포넌트 (예: `LottoForm`)
- `useCallback`: 콜백 함수 메모이제이션
- `useMemo`: 비용이 큰 계산 캐싱
- `useShallow` (Zustand): 필요한 상태만 구독

### API 최적화
- Comments API: 단일 쿼리로 트리 구조 조립 (N+1 쿼리 방지)
- TanStack Query: staleTime/gcTime 적절히 설정

### 이미지 최적화
- OG 이미지: JPEG 압축 (1.8MB → 256KB)
- `loading="lazy"` 적용

## 배포

- Vercel Serverless Functions 사용
- `api/[[...route]].ts` - API 라우트 핸들러
- 프로덕션 빌드: `npm run build`
